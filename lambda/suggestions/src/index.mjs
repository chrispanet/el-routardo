// PLACEHOLDER : remplacé par le vrai code via `make fetch-lambda`. Jamais déployé tant que
// le bloc ignore_changes de infra/lambda.tf est présent.
export const handler = async () => ({ statusCode: 501, body: "placeholder" });
